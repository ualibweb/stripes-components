import IconButton from "../IconButton";

/**
 * `PaneHeaderIconButton` is a variant of {@link IconButton} and accepts the same props.
 */
export const PaneHeaderIconButton: typeof IconButton;
export default PaneHeaderIconButton;
